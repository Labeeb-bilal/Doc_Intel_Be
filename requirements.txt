# Fixed stack per project spec. Ask before adding anything not listed here.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

sqlalchemy[asyncio]>=2.0
asyncpg>=0.30

qdrant-client>=1.12,<1.13  # pinned to match the docker-compose Qdrant server (v1.12.4)

fastembed>=0.4

pdfplumber>=0.11
python-docx>=1.1
charset-normalizer>=3.4
python-magic>=0.4.27

langchain-text-splitters>=0.3
# NOTE: langchain-text-splitters hard-depends on langchain-core (it is not
# optional). We do not import langchain / langchain-community anywhere.

tenacity>=9.0
structlog>=24.4

aioboto3>=13.2

pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
python-multipart>=0.0.9  # required by FastAPI for multipart/form file uploads

# --- Phase two: retrieval + RAG + chat ---
# LLM: Groq (openai/gpt-oss-20b), called via httpx directly — no SDK
# needed, Groq's API is a plain OpenAI-shaped REST endpoint and httpx is
# already a dependency above. (google-genai/Gemini was the original
# choice; dropped after the free-tier daily quota made it impractical.)
# fastembed already installed (ingestion stage 4) — TextCrossEncoder ships in the same package
